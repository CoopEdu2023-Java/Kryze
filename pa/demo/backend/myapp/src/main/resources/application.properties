# MySQL configuration
spring.datasource.url=jdbc:mysql://localhost:3306/TestBack?useSSL=false&serverTimezone=UTC
spring.datasource.username=Kryze
spring.datasource.password=monkey
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port (可选: 若不想使用默认的8080端口)
server.port=8080
